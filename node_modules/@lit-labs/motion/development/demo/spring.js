/**
 * @license
 * Copyright 2022 Google LLC
 * SPDX-License-Identifier: BSD-3-Clause
 */
var __decorate = (this && this.__decorate) || function (decorators, target, key, desc) {
    var c = arguments.length, r = c < 3 ? target : desc === null ? desc = Object.getOwnPropertyDescriptor(target, key) : desc, d;
    if (typeof Reflect === "object" && typeof Reflect.decorate === "function") r = Reflect.decorate(decorators, target, key, desc);
    else for (var i = decorators.length - 1; i >= 0; i--) if (d = decorators[i]) r = (c < 3 ? d(r) : c > 3 ? d(target, key, r) : d(target, key)) || r;
    return c > 3 && r && Object.defineProperty(target, key, r), r;
};
import { LitElement, html, css, } from 'lit';
import { styleMap } from 'lit/directives/style-map.js';
import { customElement, state } from 'lit/decorators.js';
import { SpringController } from '../spring.js';
export class MouseController {
    constructor(host) {
        this.position = { x: 0, y: 0 };
        this._onMouseMove = ({ clientX, clientY }) => {
            this.position = { x: clientX, y: clientY };
            this.host.requestUpdate();
        };
        (this.host = host).addController(this);
    }
    hostConnected() {
        window.addEventListener('mousemove', this._onMouseMove);
    }
    hostDisconnected() {
        window.removeEventListener('mousemove', this._onMouseMove);
    }
}
let SpringDemo = class SpringDemo extends LitElement {
    constructor() {
        super(...arguments);
        this._mouse = new MouseController(this);
        this._spring = new SpringController(this);
    }
    render() {
        const { currentValue, toValue, fromValue } = this._spring;
        return html `
      <button @click=${this._go}>Go</button>
      <div
        id="from-value"
        style=${styleMap({
            left: `calc(50% - 10px + ${fromValue * 100}px)`,
            top: `calc(50% - 10px)`,
        })}
      ></div>
      <div
        id="to-value"
        style=${styleMap({
            left: `calc(50% - 10px + ${toValue * 100}px)`,
            top: `calc(50% - 10px)`,
        })}
      ></div>
      <div
        id="current-value"
        style=${styleMap({
            left: `calc(50% - 10px + ${currentValue * 100}px)`,
            top: `calc(50% - 10px)`,
        })}
      ></div>
    `;
    }
    _go() {
        this._spring = new SpringController(this);
    }
};
SpringDemo.styles = css `
    :host {
      display: block;
      background: lightgrey;
      padding: 24px;
      box-sizing: border-box;
    }
    #current-value,
    #to-value,
    #from-value {
      position: absolute;
      width: 20px;
      height: 20px;
      border-radius: 10px;
    }
    #to-value {
      opacity: 50%;
      background: red;
    }
    #from-value {
      opacity: 50%;
      background: blue;
    }
    #current-value {
      background: black;
    }
  `;
__decorate([
    state()
], SpringDemo.prototype, "_spring", void 0);
SpringDemo = __decorate([
    customElement('spring-demo')
], SpringDemo);
export { SpringDemo };
//# sourceMappingURL=spring.js.map